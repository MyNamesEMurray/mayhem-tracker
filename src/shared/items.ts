// Poro-Snax (base and its upgrade) is handed out for free rather than bought,
// so it would otherwise land in every champion's item stats and at the front
// of every build path. Excluded from local stats, from the community upload,
// and from the build-order timeline.
export const EXCLUDED_ITEM_IDS = [2052, 220013];

export const EXCLUDED_ITEM_ID_SET = new Set(EXCLUDED_ITEM_IDS);
