node_modules/
out/
dist/
data/

# design-sync machine state (converter staging, build output, caches)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# design canvas: the seeded page is a ~2.5MB editor payload, rebuilt from the
# artboards beside it. The .dc.html sources and canvas.json are the real files.
.design-canvas/*.html
!.design-canvas/*.dc.html
